/* Write a program to print prime numbers in a range */
#include <stdio.h>
#include <math.h>
int main()
{
    int r1, r2, n, count = 0;
    printf("Enter range 1 : ");
    scanf("%d", &r1);
    printf("Enter range 1 : ");
    scanf("%d", &r2);
    for (int i = r1; i <= r2; i++)
    {
        if (i % 1 == 0 && i % i == 0)
    }
    return 0;
}