/* Search on what a "nested loop" is & print this pattern using it */
#include <stdio.h>
#include <math.h>
int main()
{
    int n;
    printf("enter n : ");
    scanf("%d", &n);
    for (int i = 0; i <= n; i++)
    {
        }
    return 0;
}