#include <stdio.h>
#include <math.h>
int main()
{

    int  d, e, f, g, h, i, j;
    int a = 21;
    int b = 32;
    int c = 24;
    // a = a + b;
    // b += c; /*this the short from of (+) like a=a+b or a=+b is same */

    // a = a - b;
    // b -= c; /*this the short from of (+) like a=a+b or a=+b is same */

    // a = a * b;
    // b *= c; /*this the short from of (+) like a=a+b or a=+b is same */

    // a = a / b;
    // b /= c; /*this the short from of (+) like a=a+b or a=+b is same */

    // a = a + b;
    // b = +c; /*this the short from of (+) like a=a+b or a=+b is same */

    printf("%d\n%d\n", a, b);
    return 0;
}