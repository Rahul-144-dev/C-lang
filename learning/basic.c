#include <stdio.h>/*it's just a liabrary for C language*/

int main()/* it's a function*/
{
    int age = 39;                   // age is a integer so that here is int before age//
    printf("the age is %d\n", age); // the age is showing and the "%d" is used for integer and the "age" is targeted to the age//
    return 0;                       // return 0;=end the  code//
}