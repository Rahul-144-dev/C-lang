#include <stdio.h>

int main()
{
    int a = 12;
    printf("the value of the a is %d\n", a);

    float pi = 3.14;
    printf("the value of the 'pi' is %f\n", pi);

    char star = '*';
    printf("the value of the a is %c\n", star);
    return 0;
}